enum Role {
    CUSTOMER
    PROVIDER
    ADMIN
}

enum UserStatus {
    ACTIVE
    BLOCKED
}

enum RentalOrderStatus {
    PENDING
    APPROVED
    REJECTED
    PAID
    RETURNED
    CANCELLED
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
}

enum PaymentProvider {
    SSLCOMMERZ
}
